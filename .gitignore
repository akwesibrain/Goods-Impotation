# Editors / OS
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Netlify
.netlify/

# Local env — Supabase keys live in supabase-client.js, but keep
# stray env files out of the repo anyway.
.env
.env.*

# Supabase CLI cache
supabase/.temp/

# Local tooling (site is static HTML — no build step)
node_modules/
